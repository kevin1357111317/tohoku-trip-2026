# Versioning Policy

目前正式版系列：`V8`。

## 進版規則

- **主版本 `Vn → V(n+1)`**：行程架構、主要日期／路線、核心 App 功能、主要版型或整體資料結構有明顯變動。
- **Patch `Vn → VnP1 → VnP2...`**：單一預約狀態、價格、文字、班次、待辦、局部 UI／樣式等小幅修正。
- 不因 service worker / cache 更新而額外增加版號；cache 必須跟正式內容使用同一版號。
- 同一次需求造成的 index、交通、預算、待辦與 cache 連動更新，視為**同一個版本**，不能拆成多個版號。
- Patch 不應無限累加；當累積修改已構成一次明顯改版時，直接升主版本。

## 目前轉換

- `V7P28` 為 V7 系列最後一版，已依政策直接進版為 `V8`（十和田湖獨木舟改訂海外旅客方案）。
- V8 之後的小修依序使用 `V8P1`、`V8P2`……；下一次重大改版則直接進 `V9`。

## 更新原則

每次修改前先確認 `main` 最新正式版與 production 一致，再依上述規則決定主版本或 Patch。不要只因修改檔案數量或 commit 數量進版。
